# Attachment - Habitat Assessment and Fish Sampling Data {-#attach-dat}

All field data collected is available `r ngr::ngr_str_link_url(url_base = params$repo_url, url_resource = "raw/main/data/", anchor_text = "here")`.

<br>

<!-- When there is fish data -->
Habitat assessment data (including fish sampling and PIT tagging information) is available for download here `r ngr::ngr_str_link_url(url_base = params$repo_url, url_resource = paste0("blob/main/data/permit_submission/", params$permit_id, ".xlsx"), anchor_text = paste0(params$repo_url, "/blob/main/data/permit_submission/", params$permit_id, ".xlsx"))`. This is the workbook submitted to the Province under the scientific fish collection permit, so it follows the provincial Fish Data Submission format. Note that habitat measurements for the small electrofishing sites nested within a crossing's assessment are not included in it - their locations and all fish data are.

<br>

Raw fish data is available for download here `r ngr::ngr_str_link_url(url_base = params$repo_url, url_resource = paste0("blob/main/data/fish_data_tags_joined_", params$project_year, ".csv"), anchor_text = paste0(params$repo_url, "/blob/main/data/fish_data_tags_joined_", params$project_year, ".csv"))`.

